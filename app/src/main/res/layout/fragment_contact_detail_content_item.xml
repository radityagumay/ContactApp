<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingEnd="26dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingStart="26dp">

    <ImageView
        android:id="@+id/iv_left_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        tools:src="@mipmap/ic_call_blue"/>

    <TextView
        android:id="@+id/tv_body_one"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/iv_left_icon"
        android:paddingLeft="28dp"
        android:paddingStart="28dp"
        android:textSize="16dp"
        tools:text="+98188383838"/>

    <TextView
        android:id="@+id/tv_body_two"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_body_one"
        android:layout_toRightOf="@+id/iv_left_icon"
        android:paddingLeft="28dp"
        android:paddingStart="28dp"
        android:paddingTop="4dp"
        android:textSize="14dp"
        tools:text="mobile"/>

    <ImageView
        android:id="@+id/iv_right_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:visibility="invisible"
        tools:src="@mipmap/ic_message"
        tools:visibility="visible"/>

</RelativeLayout>
